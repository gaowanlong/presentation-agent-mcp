# Presentation Agent MCP Environment Variables
# V0.1 uses local filesystem storage by default

# Data directory (default: ./data/)
DATA_DIR=./data/

# MCP transport (default: stdio)
MCP_TRANSPORT=stdio
